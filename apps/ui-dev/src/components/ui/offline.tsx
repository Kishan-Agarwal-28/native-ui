import { StyleSheet, Text, View } from "react-native";
const offline = () => {
  return (
    <View>
      <Text>offline</Text>
    </View>
  );
};
export default offline;
const styles = StyleSheet.create({});
